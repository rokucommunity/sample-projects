{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "brightscript",
            "request": "launch",
            "name": "BrightScript Debug: Launch",
            "rootDir": "${workspaceFolder}",
            "host": "${promptForHost}",
            "enableDebugProtocol": true
        }
    ]
}
